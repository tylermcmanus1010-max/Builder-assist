name: Stop stale Phoenix catalog v2 run

on:
  push:
    branches:
      - catalog/full-priced-products-photos-20260907-v1
    paths:
      - ".github/workflows/stop-stale-phoenix-v2.yml"
  workflow_dispatch:

concurrency:
  group: phoenix-full-priced-catalog-v2-${{ github.ref }}
  cancel-in-progress: true

jobs:
  finish:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseded by the repaired v5 catalog collector."
